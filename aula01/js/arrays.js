window.onload = function () {

    var nomes = ['jose', 'karla', 'maria'];

    for (var i = 0; i < nomes.length ; i++) {
        console.log(nomes[i]);
    }

    var i = 0;
    while (i < nomes.length) {
        console.log(nomes[i]);
        i++;
    }

}